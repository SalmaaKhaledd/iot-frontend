/** Used for production builds (`ng build`) and any import without dev file replacement. */
export const environment = {
  production: true,
  apiUrl: 'http://localhost:8080/api',
  useMock: false,
};
