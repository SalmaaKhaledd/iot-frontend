server {
    listen 80;
    listen [::]:80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    gzip on;
    gzip_vary on;
    gzip_min_length 256;
    gzip_types text/plain text/css application/json application/javascript text/javascript application/xml image/svg+xml;

    # Avoid stale shells after deploys; hashed chunk filenames remain cacheable below.
    
    location = /index.html {
        add_header Cache-Control "no-cache";
    }

    # NOTE: proxy target name must match k8s Service name exactly (see k8s/backend-service.yaml)
    location /api/ {
    proxy_pass http://iot-backend-svc:8080/api/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~* \.(?:css|js|mjs|ico|gif|jpe?g|png|webp|woff2?|svg)$ {
        expires 7d;
        add_header Cache-Control "public";
        try_files $uri =404;
    }
}
